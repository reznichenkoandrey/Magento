<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <!--
      Records go to their own file rather than system.log. The workflow this module is built for is
      "turn it on, reload one page, tail the file, turn it off", and every unrelated notice
      interleaved into that file costs the reader a scroll. It also makes cleaning up after a
      session a single rm instead of a careful grep -v.
    -->
    <virtualType name="Scr1be\FpcInspector\Logger\RecordHandler" type="Magento\Framework\Logger\Handler\Base">
        <arguments>
            <argument name="fileName" xsi:type="string">/var/log/fpc_inspector.log</argument>
        </arguments>
    </virtualType>

    <virtualType name="Scr1be\FpcInspector\Logger\RecordLogger" type="Magento\Framework\Logger\Monolog">
        <arguments>
            <argument name="name" xsi:type="string">fpcInspector</argument>
            <argument name="handlers" xsi:type="array">
                <item name="records" xsi:type="object">Scr1be\FpcInspector\Logger\RecordHandler</item>
            </argument>
        </arguments>
    </virtualType>

    <type name="Scr1be\FpcInspector\Model\Recorder">
        <arguments>
            <argument name="logger" xsi:type="object">Scr1be\FpcInspector\Logger\RecordLogger</argument>
        </arguments>
    </type>
</config>
