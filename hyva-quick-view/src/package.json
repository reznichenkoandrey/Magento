{
    "name": "scr1be-quick-view",
    "description": "ES module scope for the storefront JavaScript. The browser resolves these files relatively and the page ships no import map; this exports map exists so the specs can name the same entry points under node --test.",
    "private": true,
    "type": "module",
    "exports": {
        "./register.js": "./view/frontend/web/js/quickview-register.js",
        "./store.js": "./view/frontend/web/js/quick-view-store.js"
    },
    "scripts": {
        "test": "node --test \"Test/Js/*.test.js\""
    }
}
