<?php
declare(strict_types=1);

use Magento\Framework\Escaper;
use Scr1be\HyvaLazyImages\Block\LazyScripts;

/** @var LazyScripts $block */
/** @var Escaper $escaper */

/**
 * One external module, nothing inline — so no `HyvaCsp::registerInlineScript()` call. The
 * loader itself lives in `js/lazy-images.js` and is imported relatively by the entry module,
 * so this page ships no import map either.
 */
?>
<script type="module" src="<?= $escaper->escapeUrl($block->getEntryScriptUrl()) ?>"></script>
